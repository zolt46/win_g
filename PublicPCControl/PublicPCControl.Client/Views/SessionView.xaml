<!-- File: PublicPCControl.Client/Views/SessionView.xaml -->
<UserControl x:Class="PublicPCControl.Client.Views.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Background="#0c111c">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Background="#10192a" Padding="18" CornerRadius="16" BorderBrush="#1f2c44" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" Color="#22000000" ShadowDepth="0" />
            </Border.Effect>
            <UniformGrid Columns="5" Rows="1" Margin="0" HorizontalAlignment="Stretch">
                <StackPanel>
                    <TextBlock Text="사용자" Foreground="#92a9d8" />
                    <TextBlock Text="{Binding CurrentSession.UserName}" FontSize="19" FontWeight="Bold" Foreground="#f6fbff" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="ID" Foreground="#92a9d8" />
                    <TextBlock Text="{Binding CurrentSession.UserId}" Foreground="#f6fbff" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="이용 목적" Foreground="#92a9d8" />
                    <TextBlock Text="{Binding CurrentSession.Purpose}" Foreground="#f6fbff" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="시작 시간" Foreground="#92a9d8" />
                    <TextBlock Text="{Binding CurrentSession.StartTime, StringFormat=yyyy-MM-dd HH:mm}" Foreground="#f6fbff" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="남은 시간" Foreground="#92a9d8" />
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Remaining}" Foreground="#6dd69b" FontSize="22" FontWeight="Bold" />
                        <TextBlock Text="5분 전 알림" Foreground="#f4c066" Visibility="{Binding ShowWarning, Converter={StaticResource BoolToVis}}" Margin="10,4,0,0" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0" VerticalAlignment="Center">
                        <TextBlock Text="연장 가능" Foreground="#9fd6ff" />
                        <TextBlock Text="{Binding ExtensionsRemaining}" Foreground="#eaf2ff" FontWeight="Bold" Margin="6,0,0,0" />
                        <Button Content="연장" Command="{Binding ExtendCommand}" Padding="10,6" Background="#3a547d" Foreground="#eaf2ff" BorderBrush="#4f6fb5" BorderThickness="1" Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </UniformGrid>
        </Border>
        <Border Grid.Row="1" Background="#0e1727" BorderBrush="#1d2c45" BorderThickness="1" CornerRadius="16" Padding="18" Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="허용된 프로그램" Foreground="#e9f1ff" FontSize="18" FontWeight="Bold" />
                    <TextBlock Text="더블 클릭 또는 선택해 실행" Foreground="#9db3da" Margin="12,2,0,0" />
                    <Border Background="#13213a" BorderBrush="#25406b" BorderThickness="1" CornerRadius="10" Padding="8,4" Margin="12,0,0,0">
                        <TextBlock Text="아이콘을 클릭하면 창이 앞쪽으로 올라오도록 실행됩니다." Foreground="#b1c7ef" FontSize="12" />
                    </Border>
                </StackPanel>
                <Border Grid.Row="1" Background="#0c1423" CornerRadius="12" BorderBrush="#1d2f4b" BorderThickness="1" Padding="12" Margin="0,10,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ItemsControl ItemsSource="{Binding AllowedPrograms}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.LaunchProgramCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Cursor="Hand"
                                            Background="#111b2f"
                                            BorderBrush="#2b4065"
                                            BorderThickness="1"
                                            Margin="8"
                                            Padding="12"
                                            ToolTip="{Binding ExecutablePath}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                                                    <StackPanel HorizontalAlignment="Center" Width="120">
                                                        <Border Background="#0d1627" CornerRadius="10" Padding="6" Margin="0,0,0,6" Height="72" VerticalAlignment="Center">
                                                            <Image Source="{Binding Icon}" Width="36" Height="36" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <TextBlock Text="{Binding DisplayName}" Foreground="#f6fbff" TextAlignment="Center" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                    </StackPanel>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="허용된 프로그램이 없습니다. 관리자에게 목록을 요청해 주세요." Foreground="#9db6ff" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AllowedPrograms.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>
                <Border Background="#0f1524" Opacity="0.9" Visibility="{Binding CurrentSession, Converter={StaticResource NullToVisibilityConverter}}" CornerRadius="12" Padding="14" Grid.RowSpan="2">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="진행 중인 세션이 없습니다." Foreground="#d6e1ff" FontSize="16" Margin="0,0,0,6" />
                        <TextBlock Text="사용자 인증 후 자동으로 세션 정보와 남은 시간이 표시됩니다." Foreground="#9db6ff" TextWrapping="Wrap" TextAlignment="Center" Width="320" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="사용 종료" Padding="14,10" Command="{Binding EndCommand}" Background="#c94b4b" Foreground="White" BorderBrush="#e26b6b" BorderThickness="0" />
        </StackPanel>
    </Grid>
</UserControl>