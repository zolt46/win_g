<!-- File: PublicPCControl.Client/Views/SessionView.xaml -->
<UserControl x:Class="PublicPCControl.Client.Views.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Background="#0f1a2b">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Background="#14233a" Padding="12" CornerRadius="8">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Margin" Value="0,0,14,0" />
                    </Style>
                </StackPanel.Resources>
                <TextBlock Text="사용자:" Foreground="White" />
                <TextBlock Text="{Binding CurrentSession.UserName}" Foreground="White" FontWeight="Bold" />
                <TextBlock Text="ID:" Foreground="White" />
                <TextBlock Text="{Binding CurrentSession.UserId}" Foreground="White" />
                <TextBlock Text="남은 시간:" Foreground="LightBlue" />
                <TextBlock Text="{Binding Remaining}" Foreground="LightBlue" FontSize="20" />
                <TextBlock Text="(5분 전 경고)" Foreground="Orange" Visibility="{Binding ShowWarning, Converter={StaticResource BoolToVis}}" Margin="0" />
            </StackPanel>
        </Border>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding AllowedPrograms}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding DisplayName}" Margin="6" Padding="14,8" Command="{Binding DataContext.LaunchProgramCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="사용 종료" Padding="14,8" Command="{Binding EndCommand}" />
        </StackPanel>
    </Grid>
</UserControl>