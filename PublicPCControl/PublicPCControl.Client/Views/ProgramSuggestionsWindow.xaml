<!-- File: PublicPCControl.Client/Views/ProgramSuggestionsWindow.xaml -->
<Window x:Class="PublicPCControl.Client.Views.ProgramSuggestionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="추천 프로그램 추가" Height="560" Width="760"
        WindowStartupLocation="CenterOwner" Background="#0f1728"
        ResizeMode="CanResize"
        MaxHeight="{x:Static SystemParameters.WorkAreaHeight}"
        MaxWidth="{x:Static SystemParameters.WorkAreaWidth}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="추천 프로그램" FontSize="20" FontWeight="Bold" Foreground="#eff4ff" />
            <TextBlock Text="시작 메뉴와 바탕화면의 바로가기(.lnk)를 스캔해 실제 실행 파일이 있는 항목만 보여줍니다." Foreground="#c4d2f1" Margin="0,4,0,0" TextWrapping="Wrap" />
            <TextBlock Text="검색어를 입력하거나 새로고침 후, 추가 버튼을 누르면 허용 목록에 바로 들어갑니다." Foreground="#9fd6ff" Margin="0,2,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="추천 검색" Foreground="#9fd6ff" FontSize="12" />
                    <Grid Width="260">
                        <TextBox x:Name="SuggestionSearchBox" Width="260" Text="{Binding ProgramSearchText, UpdateSourceTrigger=PropertyChanged}" Background="#0d1627" Foreground="#f6fbff" BorderBrush="#36548c" BorderThickness="1.2" Padding="10" ToolTip="추천 프로그램 이름을 검색합니다." />
                        <TextBlock Text="이름으로 검색" Foreground="#5d76a5" Margin="12,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SuggestionSearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </StackPanel>
                <Button Content="추천 새로고침" Command="{Binding RefreshSuggestionsCommand}" Padding="12,8" Background="#24375a" Foreground="#eaf1ff" Margin="12,14,0,0" />
            </StackPanel>

            <Border Background="#0e1727" BorderBrush="#233555" BorderThickness="1" CornerRadius="12" Padding="12" Margin="0,10,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <ListBox ItemsSource="{Binding ProgramSuggestionsView}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#101a2d" CornerRadius="8" Padding="8" Margin="0,0,0,8" BorderBrush="#233555" BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Image Source="{Binding Icon}" Width="28" Height="28" Margin="0,0,10,0" />
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Foreground="#f6fbff" />
                                                <TextBlock Text="{Binding ExecutablePath}" Foreground="#b7c8f1" FontSize="12" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Text="{Binding Source}" Foreground="#9fd6ff" FontSize="12" />
                                            </StackPanel>
                                            <Button Grid.Column="2" Content="추가" Command="{Binding DataContext.UseSuggestionCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Background="#3a547d" Foreground="#eaf1ff" Padding="10,6" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock Text="표시할 추천이 없습니다. 검색어를 바꾸거나 새로고침을 눌러주세요." Foreground="#9fb4dd" FontSize="12" Margin="0,4,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ProgramSuggestions.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="닫기" Padding="14,8" Background="#1f2a3d" Foreground="#dfe8ff" Width="100" Click="OnClose" />
        </StackPanel>
    </Grid>
</Window>
