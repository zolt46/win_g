<!-- File: PublicPCControl.Client/Views/AdminView.xaml -->
<UserControl x:Class="PublicPCControl.Client.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Background="#1a1a1a">
    <Grid Margin="20" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="관리자 설정" Foreground="White" FontSize="24" />
        <Grid Grid.Row="1" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <StackPanel.Resources>
                    <Style TargetType="Control">
                        <Setter Property="Margin" Value="0,0,0,8" />
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Margin" Value="0,0,0,4" />
                    </Style>
                </StackPanel.Resources>
                <CheckBox Content="공용 PC 관리 기능 ON" IsChecked="{Binding EnforcementEnabled}" Foreground="White" />
                <CheckBox Content="관리자 전용 PC" IsChecked="{Binding IsAdminOnlyPc}" Foreground="White" />
                <CheckBox Content="허용되지 않은 프로그램 자동 종료" IsChecked="{Binding KillDisallowedProcess}" Foreground="White" />
                <TextBlock Text="기본 이용 시간(분)" Foreground="White" />
                <TextBox Text="{Binding DefaultSessionMinutes}" />
                <TextBlock Text="최대 이용 시간(분)" Foreground="White" />
                <TextBox Text="{Binding MaxSessionMinutes}" />
                <TextBlock Text="관리자 비밀번호 설정" Foreground="White" FontWeight="Bold" Margin="0,12,0,4" />
                <PasswordBox x:Name="AdminPasswordBox" PasswordChanged="OnPasswordChanged" Tag="Primary" />
                <PasswordBox x:Name="AdminPasswordConfirmBox" PasswordChanged="OnPasswordChanged" Tag="Confirm" />
                <TextBlock Text="비밀번호를 비워두면 기존 값을 유지합니다." Foreground="#c6c6c6" FontSize="12" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <StackPanel.Resources>
                    <Style TargetType="Control">
                        <Setter Property="Margin" Value="0,0,0,8" />
                    </Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Margin" Value="0,0,0,4" />
                    </Style>
                </StackPanel.Resources>
                <TextBlock Text="허용 프로그램" Foreground="White" FontWeight="Bold" />
                <DataGrid ItemsSource="{Binding AllowedPrograms}" AutoGenerateColumns="False" Height="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="이름" Binding="{Binding DisplayName}" Width="*" />
                        <DataGridTextColumn Header="경로" Binding="{Binding ExecutablePath}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Orientation="Horizontal">
                    <TextBox Width="160" Text="{Binding NewProgramName}" Margin="0,0,6,0" />
                    <TextBox Width="260" Text="{Binding NewProgramPath}" Margin="0,0,6,0" />
                    <Button Content="추가" Command="{Binding AddProgramCommand}" />
                </StackPanel>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="저장" Command="{Binding SaveCommand}" Margin="0,0,8,0" />
            <Button Content="닫기" Command="{Binding CloseCommand}" />
        </StackPanel>
    </Grid>
</UserControl>