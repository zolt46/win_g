// File: PublicPCControl.Client/Views/UserLoginView.xaml
<UserControl x:Class="PublicPCControl.Client.Views.UserLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Background="#1d1d1d">
    <Grid Margin="60">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Spacing="8">
            <TextBlock Text="사용자 정보 입력" Foreground="White" FontSize="24" />
            <TextBlock Text="이 PC 사용 기록 및 활동 정보가 저장됩니다." Foreground="LightGray" />
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="10">
                <TextBlock Text="이름" Foreground="White" />
                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="ID" Foreground="White" />
                <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="이용 목적" Foreground="White" />
                <ComboBox ItemsSource="{Binding PurposeOptions}" SelectedItem="{Binding Purpose}" />
                <TextBlock Text="희망 이용 시간(분)" Foreground="White" />
                <TextBox Text="{Binding RequestedMinutes}" />
                <CheckBox IsChecked="{Binding Consent}" Content="PC 사용 기록 저장에 동의합니다." Foreground="White" />
            </StackPanel>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="12">
                <Button Content="사용 시작" Padding="20,10" FontSize="20" Command="{Binding StartCommand}" />
                <Button Content="취소" Command="{Binding CancelCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>