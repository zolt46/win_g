<!-- File: PublicPCControl.Client/Views/UserLoginView.xaml -->
<UserControl x:Class="PublicPCControl.Client.Views.UserLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Background="#0c111c">
    <Grid Margin="64">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="0,0,0,16">
            <TextBlock Text="사용자 정보 입력" Foreground="#eff4ff" FontSize="28" Margin="0,0,0,6" FontWeight="Bold" />
            <TextBlock Text="이용 목적과 연락처를 남기고 동의 후 사용을 시작하세요." Foreground="#c4d2f1" />
        </StackPanel>
        <Border Grid.Row="1" Background="#0f1728" Padding="28" CornerRadius="18" BorderBrush="#1d2a42" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" Color="#33000000" ShadowDepth="0" />
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0">
                    <StackPanel.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,0,0,2" />
                            <Setter Property="Foreground" Value="#e2ebff" />
                            <Setter Property="FontSize" Value="14" />
                        </Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Margin" Value="0,0,0,12" />
                            <Setter Property="Padding" Value="12" />
                            <Setter Property="FontSize" Value="16" />
                            <Setter Property="Background" Value="#0b1220" />
                            <Setter Property="Foreground" Value="#f7fbff" />
                            <Setter Property="BorderBrush" Value="#3e68c3" />
                            <Setter Property="BorderThickness" Value="1.5" />
                        </Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Margin" Value="0,0,0,12" />
                            <Setter Property="Padding" Value="12" />
                            <Setter Property="FontSize" Value="16" />
                            <Setter Property="Background" Value="#0b1220" />
                            <Setter Property="Foreground" Value="#f7fbff" />
                            <Setter Property="BorderBrush" Value="#3e68c3" />
                            <Setter Property="BorderThickness" Value="1.5" />
                        </Style>
                    </StackPanel.Resources>
                    <TextBlock Text="이름" />
                    <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="ID" />
                    <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="이용 목적" />
                    <ComboBox ItemsSource="{Binding PurposeOptions}" SelectedItem="{Binding Purpose}" />
                    <TextBlock Text="희망 이용 시간 (분)" />
                    <TextBox Text="{Binding RequestedMinutes, UpdateSourceTrigger=PropertyChanged}" />
                    <Border Background="#0b1324" BorderBrush="#273552" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,4,0,0">
                        <CheckBox IsChecked="{Binding Consent}" Content="PC 사용 기록 저장에 동의합니다." Foreground="#e7efff" FontWeight="SemiBold" />
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Padding" Value="22,14" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Style>
                    </StackPanel.Resources>
                    <Button Content="사용 시작" Command="{Binding StartCommand}" Background="#4fb36a" BorderBrush="#65d186">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="#4fb36a" />
                                <Setter Property="BorderBrush" Value="#65d186" />
                                <Setter Property="Opacity" Value="0.95" />
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#23324b" />
                                        <Setter Property="BorderBrush" Value="#2f4568" />
                                        <Setter Property="Opacity" Value="0.6" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="1" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="취소" Command="{Binding CancelCommand}" Background="#1f2a3d" />
                    <Border Background="#0c1528" Padding="14" CornerRadius="12" BorderBrush="#223657" BorderThickness="1" Margin="0,6,0,0">
                        <StackPanel>
                            <TextBlock Text="입력 가이드" Foreground="#8eb6ff" FontWeight="Bold" />
                            <TextBlock Text="모든 항목을 입력하고 동의 체크 후에만 '사용 시작' 버튼이 활성화됩니다." Foreground="#dce7ff" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>