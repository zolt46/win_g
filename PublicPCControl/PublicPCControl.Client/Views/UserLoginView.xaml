<!-- File: PublicPCControl.Client/Views/UserLoginView.xaml -->
<UserControl x:Class="PublicPCControl.Client.Views.UserLoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Background="#0c111c">
    <Grid Margin="64">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" Margin="0,0,0,16">
            <TextBlock Text="사용자 정보 입력" Foreground="#eff4ff" FontSize="28" Margin="0,0,0,6" FontWeight="Bold" />
            <TextBlock Text="이용 목적과 연락처를 남기고 동의 후 사용을 시작하세요." Foreground="#c4d2f1" />
        </StackPanel>
        <Border Grid.Row="1" Background="#0f1728" Padding="28" CornerRadius="18" BorderBrush="#1d2a42" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" Color="#33000000" ShadowDepth="0" />
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0">
                    <StackPanel.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,0,0,4" />
                            <Setter Property="Foreground" Value="#e2ebff" />
                            <Setter Property="FontSize" Value="16" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Margin" Value="0,0,0,14" />
                            <Setter Property="Padding" Value="12" />
                            <Setter Property="FontSize" Value="16" />
                            <Setter Property="Background" Value="#10192c" />
                            <Setter Property="Foreground" Value="#f7fbff" />
                            <Setter Property="BorderBrush" Value="#5f85e4" />
                            <Setter Property="BorderThickness" Value="1.6" />
                        </Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Margin" Value="0,0,0,14" />
                            <Setter Property="Padding" Value="12" />
                            <Setter Property="FontSize" Value="16" />
                            <Setter Property="Background" Value="#10192c" />
                            <Setter Property="Foreground" Value="#f7fbff" />
                            <Setter Property="TextElement.Foreground" Value="#f7fbff" />
                            <Setter Property="BorderBrush" Value="#5f85e4" />
                            <Setter Property="BorderThickness" Value="1.6" />
                            <Setter Property="SelectionBrush" Value="#4fb36a" />
                            <Setter Property="CaretBrush" Value="#f7fbff" />
                            <Setter Property="HorizontalContentAlignment" Value="Left" />
                            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
                            <Setter Property="ItemContainerStyle">
                                <Setter.Value>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Background" Value="#0f1728" />
                                        <Setter Property="Foreground" Value="#f7fbff" />
                                        <Setter Property="FontSize" Value="16" />
                                    </Style>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </StackPanel.Resources>
                    <TextBlock Text="이름" />
                    <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="ID" />
                    <TextBox Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="이용 목적" />
                    <ComboBox ItemsSource="{Binding PurposeOptions}" SelectedItem="{Binding Purpose}" />
                    <Border Background="#0f1524" BorderBrush="#283b60" BorderThickness="1.2" CornerRadius="10" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="기본 이용 시간" Foreground="#9fd6ff" FontSize="14" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding SessionSummary}" Foreground="#dce7ff" TextWrapping="Wrap" FontSize="14" />
                        </StackPanel>
                    </Border>
                    <Border Background="#101a2d" BorderBrush="#2d3f64" BorderThickness="1.2" CornerRadius="12" Padding="12" Margin="0,6,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <CheckBox IsChecked="{Binding Consent}" Content="PC 사용 기록 저장에 동의합니다." Foreground="#e7efff" FontWeight="SemiBold" FontSize="15" VerticalAlignment="Center" />
                            <Button Grid.Column="1" Content="상세보기" Click="OnShowConsentDetails" Margin="10,0,0,0" Padding="10,6" Background="#213459" Foreground="#d8e6ff" BorderBrush="#3e5b93" BorderThickness="1" Cursor="Hand" />
                        </Grid>
                    </Border>
                </StackPanel>

                <Border Grid.Row="0" Grid.Column="1" Background="#0d1627" BorderBrush="#1f2f4b" BorderThickness="1" CornerRadius="14" Padding="18" Margin="18,0,0,0">
                    <StackPanel>
                        <TextBlock Text="빠른 체크" Foreground="#8eb6ff" FontSize="17" FontWeight="Bold" Margin="0,0,0,8" />
                        <TextBlock Text="예약 시간과 용도를 확인하고, 동의 후 사용을 시작하세요. 미입력 항목이 있으면 버튼이 비활성화됩니다." Foreground="#dce7ff" TextWrapping="Wrap" FontSize="15" />
                        <TextBlock Text="권장" Foreground="#9fd6ff" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,4" />
                        <Border Background="#0b1322" Padding="10" CornerRadius="10" BorderBrush="#233555" BorderThickness="1">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="실명과 연락처를 정확히 입력" Foreground="#eaf2ff" FontSize="14" />
                                <TextBlock Text="이용 목적은 가장 가까운 옵션으로 선택" Foreground="#eaf2ff" FontSize="14" />
                                <TextBlock Text="세션 종료 전 데이터 백업" Foreground="#eaf2ff" FontSize="14" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                    <Border Background="#0c1528" Padding="14" CornerRadius="12" BorderBrush="#223657" BorderThickness="1" Margin="0,0,14,0">
                        <StackPanel>
                            <TextBlock Text="입력 가이드" Foreground="#8eb6ff" FontWeight="Bold" FontSize="15" />
                            <TextBlock Text="모든 항목을 입력하고 동의 체크 후에만 '사용 시작' 버튼이 활성화됩니다." Foreground="#dce7ff" TextWrapping="Wrap" FontSize="14" />
                        </StackPanel>
                    </Border>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="취소" Command="{Binding CancelCommand}" Background="#1f2a3d" Foreground="#dfe8ff" Padding="20,12" FontSize="17" FontWeight="SemiBold" Cursor="Hand" Margin="0,0,10,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Content="사용 시작" Command="{Binding StartCommand}" Background="#4fb36a" BorderBrush="#65d186" Foreground="#0c1b0d" Padding="24,14" FontSize="19" FontWeight="Bold" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0" CornerRadius="16" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Opacity" Value="0.95" />
                                    <Style.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Background" Value="#23324b" />
                                            <Setter Property="BorderBrush" Value="#2f4568" />
                                            <Setter Property="Opacity" Value="0.6" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="1" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>