<!-- File: PublicPCControl.Client/Views/LockScreenView.xaml -->
<UserControl x:Class="PublicPCControl.Client.Views.LockScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:PublicPCControl.Client.Resources"
             mc:Ignorable="d" Background="#0c111c" Focusable="True" Loaded="OnLoaded">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <conv:InverseBooleanConverter x:Key="InverseBool" />
    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding Key="A" Modifiers="Control+Shift" Command="{Binding AdminCommand}" />
    </UserControl.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Width="620" Padding="34" Background="#121a2a" CornerRadius="18" BorderBrush="#1f2d45" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Color="#66000000" ShadowDepth="0" />
            </Border.Effect>
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,18">
                    <Border Width="12" Height="12" CornerRadius="6" Background="#4caf50" Visibility="{Binding IsUserAllowed, Converter={StaticResource BoolToVis}}" Margin="0,0,10,0" />
                    <Border Width="12" Height="12" CornerRadius="6" Background="#f0b24d" Visibility="{Binding IsUserAllowed, Converter={StaticResource InverseBool}}" Margin="0,0,10,0" />
                    <TextBlock Text="{Binding ModeLabel}" Foreground="#dce6ff" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock Text="자료열람실 공용 PC" Foreground="White" FontSize="34" HorizontalAlignment="Center" Margin="0,0,0,10" FontWeight="Bold" />
                <TextBlock Text="{Binding Notice}" Foreground="#c9d7f2" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,20"/>
                <Grid Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Background="#1c2941" CornerRadius="14" Padding="18" BorderBrush="#223551" BorderThickness="1" Grid.ColumnSpan="2">
                        <StackPanel>
                            <TextBlock Text="이용 안내" Foreground="#9cc3ff" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,6" />
                            <TextBlock Text="사용 시작을 누르면 기본 정보 입력 후 이용 시간이 측정됩니다. 모든 활동은 보안 목적에 따라 기록됩니다." Foreground="#e5ecff" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,22,0,0">
                    <Button Content="사용 시작" Padding="26,14" FontSize="22" FontWeight="Bold" Command="{Binding StartCommand}" Visibility="{Binding IsUserAllowed, Converter={StaticResource BoolToVis}}" Background="#58c16e" Foreground="#0c1b0d" BorderBrush="#74d68a" BorderThickness="0" Width="200" Margin="0,0,12,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0" CornerRadius="18" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Border Visibility="{Binding IsUserAllowed, Converter={StaticResource InverseBool}, ConverterParameter=Visibility}" Background="#2a1b10" BorderBrush="#b86a1c" BorderThickness="1" CornerRadius="12" Padding="16" Width="360">
                        <StackPanel>
                            <TextBlock Text="관리자 전용 모드" Foreground="#f2c57c" FontWeight="Bold" FontSize="16" />
                            <TextBlock Text="현재 PC는 관리자 전용으로 설정되어 있습니다. 관리자만 사용 가능합니다." Foreground="#f6e4c6" TextWrapping="Wrap" Margin="0,4,0,0" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Background="#111" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="현재 시간" Foreground="#888" VerticalAlignment="Center" />
                <TextBlock Foreground="White" VerticalAlignment="Center" Text="{Binding CurrentTime, StringFormat=HH:mm:ss}" Margin="12,0,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>